# OCR 图像识别任务表 v0.1

> 用途：记录 AzurLaneResearchTracker v0.6.0 OCR / OpenCV 图像识别层的识别目标、推荐方法、输出字段与后续截图需求。
>
> 当前基准：模拟器分辨率固定为 `1280x720`。本文档用于逐步校准和开发，不代表最终准确率；没有真实标注截图前，所有 ROI 与阈值均视为待校准。

## 1. 总体原则

- 图像识别只负责识别结果，不处理完整自动化流程。
- 港区资源识别属于轻量任务，建议每 3–5 分钟更新一次。
- 装备扫描属于重任务，建议由用户手动触发，或在明确场景下触发。
- 装备数量与碎片数量必须尽量保守：低置信度不写入，未知装备不写入。
- 整件装备数量不能边扫边直接覆盖，应先在本次扫描缓存中累加，扫描完成后再统一写入。
- 同一个装备不同强化等级仍视为同一装备，数量需要累加。
- 装备卡左下角 `+10 / +13` 是强化等级，不是装备数量。
- 装备卡右下角数字才是堆叠数量。
- 装备卡右上角“小人/装备中”标记不参与装备 ID 识别，应通过 mask 或忽略 ROI 规避。
- 碎片卡左侧数字是已有碎片数量，右侧数字是合成所需数量；写入时只使用已有碎片数量。

## 2. 总体流程任务表

| 编号 | 阶段 | 目标 | 识别方式 | 输出/动作 | 优先级 |
|---|---|---|---|---|---|
| A1 | 港区主页 | 判断当前是否在港区主页 | 固定 ROI + 港区特征模板 | `scene=harbor` | 高 |
| A2 | 港区主页 | 识别玩家名 | OCR 文本 | `player_name` | 高 |
| A3 | 港区主页 | 识别石油、物资、钻石 | OCR 数字 | `oil / coins / gems` | 高 |
| A4 | 港区主页 | 定时刷新资源 | 每 3–5 分钟截图识别 | 更新港区实况数据 | 中 |
| B1 | 港区主页 | 定位仓库入口 | 固定 ROI / 模板匹配 | 仓库点击区域 | 高 |
| B2 | 仓库页 | 定位右上角主页按钮 | 固定 ROI / 模板匹配 | 回主页点击区域 | 高 |
| B3 | 仓库页 | 定位“设计图”标签 | 固定 ROI + 颜色状态 | `design_tab_roi` / 是否选中 | 高 |
| B4 | 仓库页 | 定位“装备”标签 | 固定 ROI + 颜色状态 | `equipment_tab_roi` / 是否选中 | 高 |
| B5 | 仓库页 | 定位“筛选”标签 | 固定 ROI / 模板匹配 | `filter_button_roi` | 中 |
| B6 | 仓库页 | 定位右上角“稀有度/数量”等排序标签 | 固定 ROI + OCR/模板 | 当前排序状态 | 中 |
| C1 | 设计图页 | 确认当前处于“设计图”页 | “设计图”标签黄色判断 | `is_design_tab=True` | 高 |
| C2 | 设计图页 | 切分当前页碎片卡片 | 固定网格/轮廓检测 | 每个卡片 ROI | 高 |
| C3 | 设计图页 | 识别碎片对应装备 ID | 左侧图标匹配 `data/images` | `equipment_id` | 高 |
| C4 | 设计图页 | 识别已有碎片数量 | OCR 数字，左侧数量为主 | `fragment_count` | 高 |
| C5 | 设计图页 | 可选识别合成所需数量 | OCR 数字，右侧需求 | `required_count`，仅辅助校验 | 中 |
| C6 | 设计图页 | 翻页/下滑直到到底 | 滚动前后图像差异 + 底部判断 | 完整碎片扫描缓存 | 高 |
| C7 | 设计图页 | 写入碎片数量 | 扫描缓存完成后统一更新 | `equipment_id / fragment_count / confidence` | 高 |
| D1 | 装备页 | 点击并确认进入“装备”页 | “装备”标签黄色判断 | `is_equipment_tab=True` | 高 |
| D2 | 装备页 | 定位“装备中 OFF/ON”按钮 | 固定 ROI + 状态识别 | 按需切换到 ON | 高 |
| D3 | 装备页 | 切分当前页装备卡片 | 固定网格/轮廓检测 | 每个卡片 ROI | 高 |
| D4 | 装备页 | 识别装备 ID | 图标匹配 + mask 右上角小人 | `equipment_id` | 高 |
| D5 | 装备页 | 识别右下角堆叠数量 | 只 OCR 右下角 | `equipment_count` | 高 |
| D6 | 装备页 | 忽略左下角强化等级 | 避开 `+10/+13` ROI | 不写入数量 | 高 |
| D7 | 装备页 | 处理右上角小人标记 | mask/忽略右上角 | 不影响装备 ID | 高 |
| D8 | 装备页 | 下滑扫描所有装备 | 滚动前后图像差异 + 去重 | 完整装备扫描缓存 | 高 |
| D9 | 装备页 | 聚合同 ID 数量 | 同装备不同强化等级累加 | `equipment_count=sum(...)` | 高 |
| D10 | 装备页 | 写入整件装备数量 | 扫描完成后统一覆盖本次绝对值 | `equipment_id / equipment_count / confidence` | 高 |
| E1 | 收尾 | 点击右上角主页按钮 | 固定点击区域 | 回到港区主页 | 中 |
| E2 | 收尾 | 输出本次扫描结果 | JSON/结构化结果 | warnings / unknown / low_confidence | 中 |

## 3. 港区主页识别任务

| 识别对象 | 推荐方法 | 输出字段 | 失败策略 | 截图需求 |
|---|---|---|---|---|
| 玩家名 | 固定 ROI + OCR 文本 | `player_name` | OCR 低置信度时保留上次值并 warning | 港区主页完整 1280x720 |
| 石油 | 固定 ROI + OCR 数字 | `oil` | 低置信度不更新 | 多张资源数不同的港区截图 |
| 物资 | 固定 ROI + OCR 数字 | `coins` | 低置信度不更新 | 多张资源数不同的港区截图 |
| 钻石 | 固定 ROI + OCR 数字 | `gems` | 低置信度不更新 | 多张资源数不同的港区截图 |
| 仓库入口 | 固定 ROI / 模板匹配 | `warehouse_entry_roi` | 未识别则不点击 | 港区底部菜单截图 |

## 4. 仓库标签识别任务

| 识别对象 | 推荐方法 | 输出字段 | 用途 | 截图需求 |
|---|---|---|---|---|
| 右上角主页按钮 | 固定 ROI / 模板匹配 | `home_button_roi` | 扫描完成后回主页 | 仓库页完整截图 |
| “设计图”标签位置 | 固定 ROI | `design_tab_roi` | 切换/确认碎片页 | 设计图选中、未选中各一张 |
| “设计图”是否选中 | 颜色状态判断，黄色为选中 | `is_design_tab` | 碎片扫描前置条件 | 设计图选中截图 |
| “装备”标签位置 | 固定 ROI | `equipment_tab_roi` | 切换/确认装备页 | 装备选中、未选中各一张 |
| “装备”是否选中 | 颜色状态判断，黄色为选中 | `is_equipment_tab` | 装备扫描前置条件 | 装备选中截图 |
| “筛选”按钮 | 固定 ROI / 模板匹配 | `filter_button_roi` | 后续排序/筛选扩展 | 仓库顶部截图 |
| “稀有度/数量”排序标签 | 固定 ROI + OCR/模板 | `sort_mode` | 后续排序控制 | 排序菜单展开与收起截图 |

## 5. 碎片识别任务

| 步骤 | 目标 | 推荐方法 | 输出 | 注意事项 |
|---|---|---|---|---|
| C1 | 确认处于设计图页 | “设计图”标签黄色判断 | `is_design_tab=True` | 如果不是，先切换 |
| C2 | 切分碎片卡片 | 固定网格优先，轮廓辅助 | `fragment_card_rois` | 需要多页完整截图校准 |
| C3 | 识别装备 ID | 左侧图标匹配 `data/images` | `equipment_id` | 低置信度返回 `unknown` |
| C4 | 识别已有碎片数 | OCR 左侧/主数量 | `fragment_count` | 这是写入字段 |
| C5 | 识别合成需求数 | OCR 右侧需求 | `required_count` | 仅辅助校验，不写为用户拥有数 |
| C6 | 下滑翻页 | 滚动前后图像差异 | 下一页截图 | 需要判断是否到底 |
| C7 | 汇总写入 | 扫描缓存统一更新 | `fragment_count` | 中途失败不写正式数据 |

## 6. 整件装备识别任务

| 步骤 | 目标 | 推荐方法 | 输出 | 注意事项 |
|---|---|---|---|---|
| D1 | 确认处于装备页 | “装备”标签黄色判断 | `is_equipment_tab=True` | 如果不是，先切换 |
| D2 | 打开“装备中”显示 | 固定 ROI + 状态判断 | `show_equipped=True` | 需要 OFF/ON 截图校准 |
| D3 | 切分装备卡片 | 固定网格优先，轮廓辅助 | `equipment_card_rois` | 需要完整页截图校准 |
| D4 | 识别装备 ID | 图标匹配 + 右上角 mask | `equipment_id` | 小人标记必须忽略 |
| D5 | 识别堆叠数量 | OCR 右下角 | `equipment_count` | 数量为 1 时也要识别 |
| D6 | 忽略强化等级 | 避开左下角 ROI | 无 | `+10/+13` 不参与数量 |
| D7 | 下滑翻页 | 滚动前后图像差异 | 下一页截图 | 需要去重 |
| D8 | 同 ID 累加 | 扫描缓存聚合 | `equipment_count=sum` | 不同强化等级同 ID 累加 |
| D9 | 统一写入 | 扫描完成后覆盖本次绝对值 | `equipment_count` | 避免跨页重复/中断污染 |

## 7. 整件装备数量写入策略

整件装备不能边扫边直接覆盖正式数据。推荐流程：

1. 开始一次装备扫描，创建临时扫描缓存。
2. 每页识别所有装备卡。
3. 每张卡生成：
   - `equipment_id`
   - `equipment_count`
   - `confidence`
   - `page_index`
   - `card_roi`
   - `card_signature`
4. 对相同 `equipment_id` 累加数量。
5. 对低置信度、unknown、疑似重复卡片只记录 warning，不写入正式数据。
6. 所有页面扫描完成后，再统一写入用户数据表。

示例：

```text
G0124:
  page_1 card_3: count=9
  page_2 card_1: count=1
  final equipment_count=10
```

## 8. 小人标记与强化等级处理

| 干扰项 | 位置 | 影响 | 处理策略 |
|---|---|---|---|
| 右上角小人标记 | 装备卡右上角 | 影响图标匹配 | mask 掉右上角，不参与 ID 匹配 |
| 左下角强化等级 | 装备卡左下角 | 容易误识别成数量 | 数量 OCR 只裁右下角 |
| 星级区域 | 装备卡底部偏左 | 干扰图标/数字 | 图标 ROI 不包含星级，数量 ROI 不包含星级 |
| 卡片边框/稀有度背景 | 卡片外框 | 可能影响模板分数 | 图标匹配以装备主体为主，背景权重降低 |
| 半屏/裁切截图 | 全图 | 信息缺失 | 直接拒绝识别 |

## 9. 数据写入策略

| 数据类型 | 写入字段 | 推荐策略 | 原因 |
|---|---|---|---|
| 港区资源 | `player_name/oil/coins/gems` | 每 3–5 分钟轻量更新 | 不进仓库，风险低 |
| 碎片数量 | `fragment_count` | 设计图扫描完成后统一更新 | 防止中途失败污染数据 |
| 整件装备数量 | `equipment_count` | 装备扫描完成后按 ID 聚合统一覆盖 | 解决跨页、堆叠、强化等级问题 |
| unknown 装备 | 不写 | 只记录 warning | 避免写错装备 |
| 低置信度数字 | 不写 | 记录 warning，等待重扫 | 数量错误代价高 |

## 10. 后续截图素材需求

| 素材 | 用途 | 建议命名 |
|---|---|---|
| 港区主页完整截图 | 校准玩家名、石油、物资、钻石 ROI | `harbor_001.png` |
| 仓库标签完整截图 | 校准主页按钮、设计图、装备、筛选、稀有度位置 | `warehouse_tabs_001.png` |
| 设计图页完整截图，多页 | 校准碎片卡片切分和碎片数量 OCR | `market_frag_page_001.png` |
| 装备页完整截图，多页 | 校准装备卡片切分和装备数量 OCR | `market_eq_page_001.png` |
| “装备中 OFF”截图 | 校准装备中按钮关闭状态 | `equipped_filter_off_001.png` |
| “ON装备中”截图 | 校准装备中按钮开启状态 | `equipped_filter_on_001.png` |
| 带右上角小人标记的装备卡 | 做 mask，避免影响装备 ID | `eq_card_equipped_Gxxxx_001.png` |
| 不带小人标记的同装备卡 | 对照测试图标匹配稳定性 | `eq_card_plain_Gxxxx_001.png` |
| 已知装备 ID 的单卡截图 | 校准图标匹配和误识别阈值 | `eq_type_G0124_001.png` |

## 11. 推荐开发顺序

1. 仓库标签识别：
   - 主页按钮
   - 设计图标签
   - 装备标签
   - 筛选按钮
   - 稀有度/数量排序标签
2. 港区资源 OCR：
   - 玩家名
   - 石油
   - 物资
   - 钻石
3. 单卡数字 OCR：
   - 装备右下角数量
   - 碎片已有数量
   - 碎片合成需求数量
4. 单卡装备 ID 识别：
   - 图标匹配
   - 小人标记 mask
   - unknown / ambiguous 策略
5. 完整页卡片切分：
   - 设计图页卡片
   - 装备页卡片
6. 完整仓库扫描：
   - 下滑翻页
   - 到底判断
   - 去重
   - 聚合
7. 统一写入用户数据：
   - 碎片数量
   - 整件装备数量
   - warnings / skipped records

## 12. 当前优先任务

建议下一步优先做 `B 仓库标签识别`：

1. 识别右上角主页按钮。
2. 识别“设计图 / 装备”标签位置。
3. 判断“设计图”是否黄色选中。
4. 判断“装备”是否黄色选中。
5. 识别“筛选”和“稀有度”标签位置。

这一步完成后，碎片识别和整件装备识别才有稳定入口。
